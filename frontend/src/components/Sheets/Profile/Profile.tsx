import React from 'react';
import { CreateStyledCell } from '../CreateStyledCell';

export function Profile(sx: number, sy: number, zoom: number) {
  return (
    <div key={"profile"}>
    </div>);
}
